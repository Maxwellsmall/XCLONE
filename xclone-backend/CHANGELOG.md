# Changelog

## [Unreleased]
### Added
- User registration and JWT authentication endpoints (`/api/auth/`).
- `Tweet` model and feed list view (`/api/tweets/`).

### Changed
- Refactored `User` model to use email instead of username for login.